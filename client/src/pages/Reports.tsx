import { BarChart3, Download, FileBarChart2, ShieldCheck, TimerReset } from "lucide-react";
import DashboardLayout from "@/components/DashboardLayout";

const reportRows = [{ name: "Transformer health", figure: "97.1%", detail: "Fleet reporting availability", tone: "positive" }, { name: "Open ticket SLA", figure: "100%", detail: "No current overdue tickets", tone: "positive" }, { name: "Priority notices", figure: "03", detail: "1 high and 2 watch events", tone: "attention" }, { name: "Coverage population", figure: "9,000+", detail: "Active service connections", tone: "neutral" }];
export default function Reports() { return <DashboardLayout eyebrow="CHENNAI CITY / PERFORMANCE" title="Operations Reports"><section className="page-intro with-action"><div><span className="section-label">Decision support</span><h2>Operational performance across the authorised city scope.</h2><p>Use scheduled reports and audit-ready summaries to identify recurring risk and workforce demand.</p></div><button className="secondary-action"><Download size={16} /> Export report</button></section><section className="report-metrics">{reportRows.map((row) => <article key={row.name}><span className={`report-icon ${row.tone}`}>{row.name === "Transformer health" ? <ShieldCheck size={18} /> : row.name === "Open ticket SLA" ? <TimerReset size={18} /> : row.name === "Priority notices" ? <FileBarChart2 size={18} /> : <BarChart3 size={18} />}</span><div><small>{row.name}</small><strong>{row.figure}</strong><p>{row.detail}</p></div></article>)}</section><section className="report-layout"><article className="section-card report-chart"><header><div><span className="section-label">Four-week trend</span><h3>Transformer health stability</h3></div><span className="report-key">City scope · Chennai</span></header><div className="chart-bars">{[92, 95, 94, 97, 96, 98, 97].map((value, index) => <div key={index}><i style={{ height: `${value}%` }} /><span>W{index + 1}</span></div>)}</div><div className="chart-rule"><span>Health baseline</span><b>95%</b></div></article><article className="section-card recurring-card"><span className="section-label">Recurring signal</span><h3>Watch list by diagnostic region</h3><div><span>Thermal</span><b>01</b><i style={{ width: "35%" }} /></div><div><span>Loading</span><b>01</b><i style={{ width: "35%" }} /></div><div><span>Electrical</span><b>01</b><i style={{ width: "55%" }} /></div><p>Electrical power-factor behaviour is the highest priority for the current reporting period.</p></article></section></DashboardLayout>; }
